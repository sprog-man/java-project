spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_companion_platform_prod?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password:
    database: 2

logging:
  level:
    root: warn
    com.petcompany.platform: info

server:
  port: 80
